"""Reading the Bingo Brawlers broadcast overlay off a single frame.

Everything that touches pixels lives here: the fractional geometry (layout), the
tesseract wrapper (ocr), and one module per overlay element read through them.
"""
